; Testing Setting INI files -- No sequence; multiple table settings, so
;   can't use single table setting as implicit sequence (use single
;   table setting for all actions).

tableName = "albums"

[ AddView ]

tableName = "albums"

tableTitle = "Albums"
tableDescription = "A table of albums and artists"
tableFootnote = "A footnote about this table..."

field.id.label = "id"
field.id.hide = 1

field.artist.label = "Artist"

field.title.label = "Album Title"

[ ModifyingView ]

tableName = "albums"

tableTitle = "Albums"
tableDescription = "A table of albums and artists"
tableFootnote = "A footnote about this table..."

field.id.label = "id"
field.id.hide = 1

field.artist.label = "Artist"

field.title.label = "Album Title"
