; Testing Setting INI files -- Bad top-level property

sequence.initAction = "displayAll"

sequence.setting = TableSetting

badProperty = "Invalid top-level property"

[TableSetting]

field.id.label = "id"
field.id.hide = 1

field.artist.label = "Artist"

field.title.label = "Album Title"
