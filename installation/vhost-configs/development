#
# Virtual Host Examples for Ramp/Smart
# 
# In the examples below, the DocumentRoot for the Ramp or Smart
# databases should be the appropriate ".../ramp/public" directory under
# either the server's overall DocumentRoot directory
# (/Library/WebServer/Documents in the example below) or a developer's
# or administrator's personal DocumentRoot directory
# (/Users/abrady/WebApplications in the examples below).
#
# You can name your servers whatever you want (the examples below set up
# servers called ramp.demo, smart.demo, and ramp.development).  The
# APPLICATION_ENV variable for each server should match the name of a
# section in your application/configs/application.ini file.  For
# example, the rampdemo APPLICATION_ENV for the ramp.demo server below
# matches the rampdemo section in the rampDemo.ini file in the
# application/configs directory, while the development APPLICATION_ENV
# for the ramp.development server matches the development section in
# the rampApplicationTemplate.ini file.  If you do not set an
# APPLICATION_ENV variable, its value will default to 'production'.
#
#

#
# Use name-based virtual hosting.
#
NameVirtualHost *:80

#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#
# <VirtualHost *:80>
#     ServerAdmin webmaster@dummy-host.example.com
#     DocumentRoot "/usr/docs/dummy-host.example.com"
#     ServerName dummy-host.example.com
#     ServerAlias www.dummy-host.example.com
#     ErrorLog "/private/var/log/apache2/dummy-host.example.com-error_log"
#     CustomLog "/private/var/log/apache2/dummy-host.example.com-access_log" common
# </VirtualHost>

<VirtualHost *:80>
   ServerName localhost
   DocumentRoot "/Library/WebServer/Documents"
</VirtualHost>

<VirtualHost *:80>
      ServerName rampdemo
      DocumentRoot /var/www/ramp/public
      ErrorLog /var/log/ramp/error.log
      CustomLog /var/log/ramp/access.log combined
      SetEnv APPLICATION_ENV rampdemo
      <Directory "/var/www/ramp/public">
          Options MultiViews SymlinksIfOwnerMatch
          AllowOverride All
          Order allow,deny
          Allow from all
      </Directory>
 </VirtualHost>

<VirtualHost *:80>
      ServerName smartdemo
      DocumentRoot /var/www/ramp/public
      ErrorLog /var/log/ramp/error.log
      CustomLog /var/log/ramp/access.log combined
      SetEnv APPLICATION_ENV smartdemo
      <Directory "/var/www/ramp/public">
          Options MultiViews SymlinksIfOwnerMatch
          AllowOverride All
          Order allow,deny
          Allow from all
      </Directory>
 </VirtualHost>

<VirtualHost *:80>
      ServerName development
      DocumentRoot /var/www/ramp/public
      ErrorLog /var/log/ramp/error.log
      CustomLog /var/log/ramp/access.log combined
      SetEnv APPLICATION_ENV development
      <Directory "/var/www/ramp/public">
          Options MultiViews SymlinksIfOwnerMatch
          AllowOverride All
          Order allow,deny
          Allow from all
      </Directory>
 </VirtualHost>

# Production environment does not need SetEnv APPLICATION_ENV production
<VirtualHost *:80>
      ServerName smart.njala.edu.sl
      ServerAlias www.smart.njala.edu.sl
      DocumentRoot /var/www/ramp/public
      ErrorLog /var/log/ramp/error.log
      CustomLog /var/log/ramp/access.log combined
      <Directory "/var/www/ramp/public">
          Options MultiViews SymlinksIfOwnerMatch
          AllowOverride All
          Order allow,deny
          Allow from all
      </Directory>
 </VirtualHost>

<VirtualHost *:80>
      ServerName test-environment
      DocumentRoot /var/www/ramp/public
      ErrorLog /var/log/ramp/error.log
      CustomLog /var/log/ramp/access.log combined
      SetEnv APPLICATION_ENV testing
      <Directory "/var/www/ramp/public">
          Options MultiViews SymlinksIfOwnerMatch
          AllowOverride All
          Order allow,deny
          Allow from all
      </Directory>
 </VirtualHost>

<VirtualHost *:80>
      ServerName next-version
      DocumentRoot /var/www/ramp/public
      ErrorLog /var/log/ramp/error.log
      CustomLog /var/log/ramp/access.log combined
      SetEnv APPLICATION_ENV staging
      <Directory "/var/www/ramp/public">
          Options MultiViews SymlinksIfOwnerMatch
          AllowOverride All
          Order allow,deny
          Allow from all
      </Directory>
 </VirtualHost>

