<?php
$url = array();
if ( isset($this->url) && $this->url != "" )
{
    $url = $this->url;
    if ( isset($url['activity']) && $url['activity'] != "" )
        $url['activity'] = urlencode($url['activity']);
}
$this->url = $this->url($url, null, true);
?>
<li>
    <a href="<?php echo $this->url ?>">
        <?php echo $this->escape($this->title); ?></a>
</li>
