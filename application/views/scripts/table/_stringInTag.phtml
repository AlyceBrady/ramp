<?php
  /* This helper script expects an array with the following:
   *    - a string ('value')
   *    - the keyword for an HTML tag, e.g., 'p' or 'li' ('tag')
   */

$tag = $this->escape($this->tag);

if ( strlen($this->value) > 0 ) :
    echo "<$tag;>";
    echo $this->escape($this->value);
    echo "</$tag>";
endif;

?>

