; This file has:
;   - NO VISIBLE FIELDS in the table setting
;   [Also, some not particularly important facts:]
;       - explicit sequence information
;       - displayAll and detail using same setting
;       - sequence specifying setting defined in same file
;       - table setting with section name, different from file "setting" name

sequence.initAction = "displayAll"

sequence.setting = TableSetting1
sequence.searchResultsSetting = TableSetting1

[TableSetting1]
tableName = "ramp_auth_users"
tableTitle = "RAMP Users Table"
tableDescription = "Actual table being used by RAMP"
tableFootnote = "Actual table being used by RAMP, so be careful adding or deleting!"
tableInitialSortField = "username"
