; Tests various strange configuration properties for a table setting.

tableName = "Student"

; initAction can be "displayAll" or "search"
sequence.initAction = "displayAll"

sequence.setting = DetailedView

; The following are table settings, rather than sequence settings

[ DetailedView ]

tableName = "Student"

tableTitle = "Reference With No Properties Test"
tableDescription = "Table has an external reference with no sub-properties
defined (msg refers to no viewingSequence property)."

field.studentID.label = "id"
field.studentID.footnote = "set automatically; do not update!"

externalTableRef.abc = "NoProperties"
