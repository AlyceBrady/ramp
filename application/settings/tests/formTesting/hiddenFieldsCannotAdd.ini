; This file specifies a sequence with:
;   - a list as the starting point (not a search)
;   - a search results that shows all fields
;   - a main setting view constrained to a subset of fields (summary info)
;   - broken Add functionality, because user cannot provide needed
;     information for some required fields
;   - many footnotes, tooltips, color clues, error messages: see
;     individual Table Setting files for more info

sequence.initAction = "displayAll"

sequence.setting = tests/formTesting/PlacesAbbreviated
sequence.searchResultsSetting = tests/formTesting/PlacesComplete
